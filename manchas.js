
class Manchas{


    constructor(){
this.x = 50
this.y = 50
this.lado = 50

    }

    dibujar(){


        rectMode(CENTER);
        fill(r,g,b,a)

        rect(this.x, this.y , this.lado, this.lado);
        
  }
      dibujar1(){


        rectMode(CENTER);
        fill(r,g,b,a)

        rect(this.x, this.y , this.lado, this.lado);
        
  }
      dibujar2(){

        rectMode(CENTER);
        fill(r,g,b,a)

        rect(this.x, this.y , this.lado, this.lado);
        
  }
      dibujar4(){


        rectMode(CENTER);
        fill(r,g,b,a)

        rect(this.x, this.y , this.lado, this.lado);
        
  }
  cambiocolor(){
    
      r = random(255);
  g = random(255); 
  b = random(255); 
  }
  
  
}